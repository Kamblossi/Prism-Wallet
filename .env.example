# Prism Wallet environment example

# Application
APP_URL=https://CHANGE-ME.onrender.com
AUTH_PROVIDER=local

# Database (choose either DATABASE_URL or individual fields)
#DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DB_HOST=localhost
DB_PORT=5432
DB_NAME=prism_wallet_prod
DB_USER=postgres
DB_PASSWORD=postgres
# For managed Postgres such as Supabase/Neon
DB_SSLMODE=require
# Set to 1 to skip Docker-specific hostaddr resolution
DB_DISABLE_HOSTADDR=1

# Timezone
TZ=Africa/Nairobi

# Storage
# local (default) or supabase
STORAGE_DRIVER=local
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_STORAGE_BUCKET=logos

# Optional: SMTP defaults (can be configured via Admin UI as well)
SMTP_ADDRESS=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=
SMTP_ENCRYPTION=
# Prism Wallet - Example Environment File

# App
APP_URL=http://localhost:8081
TZ=Africa/Nairobi

# Database (Docker compose uses these defaults)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=prism_wallet_dev
DB_USER=postgres
DB_PASSWORD=postgres

# Development only: bypass auth entirely (not recommended for production)
DISABLE_AUTH=0
